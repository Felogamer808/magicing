import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Soldaduras y chapas",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
