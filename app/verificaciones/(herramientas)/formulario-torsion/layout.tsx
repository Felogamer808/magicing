import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Formulario de torsión",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
