import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Longitudes de anclaje y solape",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
