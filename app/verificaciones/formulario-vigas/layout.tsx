import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Formulario de vigas — reacciones, cortante, flector y flecha",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
