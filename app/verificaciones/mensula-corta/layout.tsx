import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Ménsula corta — bielas y tirantes",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
