import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Steel deck — Flexión y fuego",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
